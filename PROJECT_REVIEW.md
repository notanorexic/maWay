# Анализ проекта и рекомендации по исправлению

## 📋 Обзор найденных проблем

### 1. **project_mk.1/index.html**

#### Критические ошибки:

**Строка 6:** Опечатка в атрибуте viewport
```html
<!-- НЕПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width" inicial-scale="1.0" />

<!-- ПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```
**Объяснение:** `inicial-scale` должно быть `initial-scale`, и значение должно быть внутри `content` через запятую.

**Строка 7:** Неподходящий заголовок страницы
```html
<title>BDSM</title>
```
**Объяснение:** Название не соответствует содержимому формы. Рекомендуется: "Форма регистрации" или "Практика формы".

**Строка 34:** Неправильный тип input для возраста
```html
<!-- НЕПРАВИЛЬНО -->
<input type="text" name="age" id="age" inputmode="numeric" maxlength="3" required />

<!-- ПРАВИЛЬНО -->
<input type="number" name="age" id="age" min="1" max="150" required />
```
**Объяснение:** Для числовых значений следует использовать `type="number"` вместо `type="text"` с `inputmode="numeric"`.

**Строка 75:** Пустой атрибут class и неправильный атрибут textarea
```html
<!-- НЕПРАВИЛЬНО -->
<div class="">
    <label for="comm">Расскажите о себе:<textarea type="text" name="comm" id="comm" required ></textarea></label>
</div>

<!-- ПРАВИЛЬНО -->
<div>
    <label for="comm">Расскажите о себе:</label>
    <textarea name="comm" id="comm" required></textarea>
</div>
```
**Объяснение:** 
- `textarea` не имеет атрибута `type`
- Пустой `class=""` следует удалить
- Лучше вынести `label` отдельно для лучшей доступности

---

### 2. **project_mk.1/css/styles.css**

**Строка 179:** Отсутствует точка с запятой
```css
/* НЕПРАВИЛЬНО */
button:hover {
    background-color: #ffb0ee;
    box-shadow: 0 4px 16px rgb(240, 160, 213),
                0 0 25px rgb(236, 107, 178)  /* <-- нет точки с запятой */
}

/* ПРАВИЛЬНО */
button:hover {
    background-color: #ffb0ee;
    box-shadow: 0 4px 16px rgb(240, 160, 213),
                0 0 25px rgb(236, 107, 178);
}
```
**Объяснение:** В CSS каждая строка должна заканчиваться точкой с запятой.

---

### 3. **festival_project_actual/index.html**

**Строка 11:** Запятая в неправильном месте в viewport
```html
<!-- НЕПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width" , initial-scale="1.0" />

<!-- ПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```
**Объяснение:** Запятая должна быть внутри значения `content`, а не между атрибутами.

**Строка 20, 78, 82, 86, 129:** Абсолютные пути к изображениям
```html
<!-- НЕПРАВИЛЬНО -->
<img class="sun_hands" src="/images/sun_hands.jpg" alt="" />

<!-- ПРАВИЛЬНО -->
<img class="sun_hands" src="../images/sun_hands.jpg" alt="" />
```
**Объяснение:** Абсолютные пути (`/images/`) работают только на сервере с корневым каталогом. Для локальной разработки нужны относительные пути (`../images/`).

**Строка 28, 34, 40:** Абсолютные пути в ссылках
```html
<!-- НЕПРАВИЛЬНО -->
<a href="/track1" class="palka_link" target="_blank">

<!-- ПРАВИЛЬНО (если это реальные страницы) -->
<a href="./track1.html" class="palka_link" target="_blank">
<!-- ИЛИ если это внешние ссылки -->
<a href="https://example.com/track1" class="palka_link" target="_blank">
```

**Строка 48:** Незакрытый тег `<time>`
```html
<!-- НЕПРАВИЛЬНО -->
<time datetime="2025-12-31"> 31 декабря</time><b> Где:</b>

<!-- ПРАВИЛЬНО -->
<time datetime="2025-12-31">31 декабря</time> <b>Где:</b>
```
**Объяснение:** Тег закрыт правильно, но лучше добавить пробел перед следующим элементом.

**Строка 56:** Лишний закрывающий тег `</div>`
```html
<!-- Проверить структуру: открывающих и закрывающих тегов должно быть поровну -->
```

**Строка 58, 61:** Пустые атрибуты
```html
<!-- НЕПРАВИЛЬНО -->
<main class="">
<div>

<!-- ПРАВИЛЬНО -->
<main>
<div>
```
**Объяснение:** Пустые атрибуты `class=""` следует удалить.

**Строка 20:** Отсутствует alt-текст для изображения
```html
<!-- НЕПРАВИЛЬНО -->
<img class="sun_hands" src="/images/sun_hands.jpg" alt="" />

<!-- ПРАВИЛЬНО -->
<img class="sun_hands" src="../images/sun_hands.jpg" alt="Изображение солнца с руками" />
```
**Объяснение:** Атрибут `alt` обязателен для доступности. Он должен описывать содержимое изображения.

---

### 4. **festival_project_actual/css/poster.css**

**Строки 78, 82, 86:** Абсолютные пути в background-image
```css
/* НЕПРАВИЛЬНО */
.palka_one {
    background-image: url("/images/handsome_guy.jpg");
}

/* ПРАВИЛЬНО */
.palka_one {
    background-image: url("../images/handsome_guy.jpg");
}
```
**Объяснение:** Аналогично HTML, используйте относительные пути.

**Строка 129:** Абсолютный путь в background
```css
/* НЕПРАВИЛЬНО */
background: url("/images/forest.jpg") center/cover no-repeat fixed;

/* ПРАВИЛЬНО */
background: url("../images/forest.jpg") center/cover no-repeat fixed;
```

---

### 5. **festival_project_actual/content.html**

**Строка 6:** Запятая в viewport
```html
<!-- НЕПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width", initial-scale="1.0" />

<!-- ПРАВИЛЬНО -->
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
```

**Строка 9:** Опечатка в теге link
```html
<!-- НЕПРАВИЛЬНО -->
<ling rel="stylesheet" href="./css/content.css" />

<!-- ПРАВИЛЬНО -->
<link rel="stylesheet" href="./css/content.css" />
```
**Объяснение:** `ling` должно быть `link`.

**Строка 15:** Незакрытая кавычка в атрибуте class
```html
<!-- НЕПРАВИЛЬНО -->
<label class="card-table>

<!-- ПРАВИЛЬНО -->
<label class="card-table">
```

**Строки 20, 25, 30, и далее:** Незакрытые теги `<label>` и `<input>`
```html
<!-- НЕПРАВИЛЬНО -->
<label class="card-table"
    <input type="checkbox">

<!-- ПРАВИЛЬНО -->
<label class="card-table">
    <input type="checkbox">
```
**Объяснение:** Все теги должны быть правильно закрыты. У `<label>` должна быть закрывающая `>`, а у `<input>` может быть самозакрывающийся тег `/>` или просто `>`.

**Строка 262:** Незакрытый тег `<p>`
```html
<!-- НЕПРАВИЛЬНО -->
<p>Рейтинг<p>

<!-- ПРАВИЛЬНО -->
<p>Рейтинг</p>
```

**Строка 472:** Неправильная структура HTML - `<li>` внутри `<tr>`
```html
<!-- НЕПРАВИЛЬНО -->
<table>
    <tr>
        <li>Возрастные ограничения: 16+</li>
    </tr>
</table>

<!-- ПРАВИЛЬНО -->
<table>
    <tr>
        <td>Возрастные ограничения: 16+</td>
    </tr>
</table>
<!-- ИЛИ если это список -->
<ul>
    <li>Возрастные ограничения: 16+</li>
</ul>
```
**Объяснение:** Внутри `<tr>` должны быть `<td>` или `<th>`, а не `<li>`.

---

### 6. **html-projects/survey-form/build-a-survey-form.html**

**Строка 60:** Неправильно закрытый тег `<option>`
```html
<!-- НЕПРАВИЛЬНО -->
<option value="select current role" disabled selected />
Select current role
</option>

<!-- ПРАВИЛЬНО -->
<option value="select current role" disabled selected>
Select current role
</option>
```
**Объяснение:** Тег `<option>` не самозакрывающийся. Текст должен быть внутри тега.

**Строка 71, 74, 77:** Одинаковые значения для разных radio-кнопок
```html
<!-- НЕПРАВИЛЬНО -->
<input type="radio" name="recommendation" value="recommendation" checked />
<input type="radio" name="recommendation" value="recommendation" />

<!-- ПРАВИЛЬНО -->
<input type="radio" name="recommendation" value="definitely" checked />
<input type="radio" name="recommendation" value="maybe" />
<input type="radio" name="recommendation" value="not-sure" />
```
**Объяснение:** Каждая radio-кнопка должна иметь уникальное значение `value`.

**Строка 81:** Опечатка "fearute" → "feature"
```html
<!-- НЕПРАВИЛЬНО -->
<p>What is your favorite fearute of freeCodeCamp?</p>

<!-- ПРАВИЛЬНО -->
<p>What is your favorite feature of freeCodeCamp?</p>
```

**Строка 97:** Опечатка "improoved" → "improved"
```html
<!-- НЕПРАВИЛЬНО -->
<p>What would you like to see improoved? (Check all that apply)</p>

<!-- ПРАВИЛЬНО -->
<p>What would you like to see improved? (Check all that apply)</p>
```

**Строка 138:** Отсутствует атрибут `for` в label
```html
<!-- НЕПРАВИЛЬНО -->
<label for="">Homemade Porn</label>

<!-- ПРАВИЛЬНО -->
<label for="homemade-porn">Homemade Porn</label>
```

**Строка 146:** Опечатка "Negarivity" → "Negativity"
```html
<!-- НЕПРАВИЛЬНО -->
<label for="less-negarivity">Less Negarivity</label>

<!-- ПРАВИЛЬНО -->
<label for="less-negativity">Less Negativity</label>
```

**Строка 179:** Опечатка в закрывающем теге `</secton>` → `</section>`

---

### 7. **html-projects/online_newspaper/online_newspaper_SVETskiy_soyuz.html**

**Строка 22:** Пустой тег `<time>` без содержимого
```html
<!-- НЕПРАВИЛЬНО -->
<time datetime="2025-11-17"></time>

<!-- ПРАВИЛЬНО -->
<time datetime="2025-11-17">17 ноября 2025</time>
```

**Строка 26:** `<fieldset>` вне формы
```html
<!-- НЕПРАВИЛЬНО -->
</header>
</body>

<fieldset>
    <nav>
```

**Объяснение:** `<fieldset>` должен быть внутри `<form>`. Если это просто навигация, используйте `<nav>` без `<fieldset>`.

**Строка 29-35:** Незакрытые теги `<li>`
```html
<!-- НЕПРАВИЛЬНО -->
<li><a href="#movies_series-2025">Кино и Сериалы 2025</a>
<li><a href="#books">Книги</a>

<!-- ПРАВИЛЬНО -->
<li><a href="#movies_series-2025">Кино и Сериалы 2025</a></li>
<li><a href="#books">Книги</a></li>
```

**Строка 41:** Несоответствие id и href
```html
<!-- НЕПРАВИЛЬНО -->
<section id="movies-series-2025">
<!-- но в навигации -->
<a href="#movies_series-2025">

<!-- ПРАВИЛЬНО -->
<section id="movies-series-2025">
<a href="#movies-series-2025">
```
**Объяснение:** ID и href должны точно совпадать.

**Строка 123:** Неправильное использование `for` в `<p>`
```html
<!-- НЕПРАВИЛЬНО -->
<p for="comment">Ваш комментарий</p>

<!-- ПРАВИЛЬНО -->
<label for="comment">Ваш комментарий</label>
```
**Объяснение:** Атрибут `for` используется только в `<label>`, не в `<p>`.

**Строка 311:** Несоответствие id в href и самом элементе
```html
<!-- НЕПРАВИЛЬНО -->
<article id="a_working_man">
<!-- но в навигации -->
<a href="#a-working-man">

<!-- ПРАВИЛЬНО -->
<article id="a-working-man">
<a href="#a-working-man">
```

**Строка 448:** Устаревший тег `<center>`
```html
<!-- НЕПРАВИЛЬНО -->
<h3><center><a href="...">Последнее желание</a></center></h3>

<!-- ПРАВИЛЬНО -->
<h3 style="text-align: center;"><a href="...">Последнее желание</a></h3>
<!-- ИЛИ лучше через CSS -->
```
**Объяснение:** Тег `<center>` устарел в HTML5. Используйте CSS.

---

## 📝 Общие рекомендации

### Доступность (Accessibility)
1. **Всегда добавляйте alt-текст к изображениям** - это критично для скринридеров
2. **Используйте семантические HTML5 теги** - `<header>`, `<main>`, `<nav>`, `<section>`, `<article>`, `<footer>`
3. **Связывайте label с input через атрибут `for`** - улучшает UX и доступность

### Валидность HTML
1. **Проверяйте HTML через валидатор** - https://validator.w3.org/
2. **Закрывайте все теги правильно**
3. **Не используйте устаревшие теги** - `<center>`, `<font>`, и т.д.

### Пути к файлам
1. **Используйте относительные пути** для локальной разработки
2. **Проверяйте пути** - они должны быть относительно текущего файла

### CSS
1. **Всегда ставьте точку с запятой** в конце CSS-правил
2. **Используйте относительные пути** в `url()`

### Формы
1. **Используйте правильные типы input** - `number` для чисел, `email` для email
2. **Уникальные значения для radio-кнопок** в одной группе
3. **Связывайте label с input** через `for` и `id`

---

## ✅ Приоритет исправлений

### 🔴 Критично (ломает функциональность):
1. Незакрытые теги в `content.html`
2. Опечатка `ling` → `link` в `content.html`
3. Незакрытая кавычка в `content.html` строка 15
4. Неправильная структура `<li>` в `<tr>` в `online_newspaper_SVETskiy_soyuz.html`

### 🟡 Важно (влияет на работу):
1. Абсолютные пути к изображениям
2. Опечатки в viewport meta-тегах
3. Неправильные типы input (text вместо number для возраста)
4. Отсутствие alt-текста у изображений

### 🟢 Желательно (улучшает качество):
1. Удаление пустых атрибутов class
2. Исправление опечаток в тексте
3. Замена устаревших тегов
4. Улучшение семантики HTML

